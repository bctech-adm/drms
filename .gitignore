node_modules/
.next/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
.env
.env.*
!.env.example
# local media (dev)
apps/web/media/
# throwaway test env secrets (apps/web/test-env/up.sh) are covered by `.env` above
# real seed data (SEED_DATA_FILE) — never commit
*.seed-data.json
seed-data.*.json
# PDF RAM measurement scratch dir (tests/integration/form-228.int.test.ts)
apps/web/.pdf-rss-*/
# go-live import (E11): client workbooks, reports and Keycloak user lists — never commit (personal data)
impor-dry-run-*.json
impor-commit-*.json
*-keycloak-users.json
*-keycloak-users.csv
kc-map*.csv
*.klien.xlsx
